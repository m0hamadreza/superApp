{
  "comment": "External micro-frontend remotes the host loads at runtime but does not develop. `pnpm setup:remotes` clones (or pulls) each one as a sibling of the monorepo and pre-builds a static bundle. Serve each build/generated/<platform> dir on its port (see mprocs/host*.yaml).",
  "remotes": [
    {
      "name": "news",
      "repo": "https://github.com/m0hamadreza/news.git",
      "dir": "news",
      "port": 9004
    },
    {
      "name": "booking",
      "repo": "https://github.com/m0hamadreza/booking.git",
      "dir": "booking",
      "port": 9000
    }
  ]
}
